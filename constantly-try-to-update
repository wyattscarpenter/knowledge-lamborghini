while true; do
  if [ "`git pull origin master --rebase`" != "Already up to date." ]; then
    pkill node
    npm start &
  fi
  #sleep 1 #fudge factor to prevent git from humiliatingly erroring.
done